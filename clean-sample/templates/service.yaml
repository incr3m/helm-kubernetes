apiVersion: v1
kind: Service
metadata:
  name: {{ template "clean-sample.fullname" . }}
  labels:
    app: {{ template "clean-sample.name" . }}
    chart: {{ template "clean-sample.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "clean-sample.name" . }}
    release: {{ .Release.Name }}
